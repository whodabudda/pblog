<!DOCTYPE html>
<html lang="en">
  <head>
      <meta charset="utf-8" />
      <meta http-equiv="X-UA-Compatible" content="IE=edge" />
      <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <!-- see if this prevents js from executing twice-->
      <meta name="turbolinks-cache-control" content="no-preview" />
    <title>Pblog</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_packs_with_chunks_tag 'app', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_packs_with_chunks_tag 'app','pblog','ckeditor', 'data-turbolinks-track': 'reload' %>
 
    <link rel="manifest" href="/manifest.json" />
    <meta name="apple-mobile-web-app-capable" content="yes">
  </head>
  <body>
      <%= render partial: "shared/navbar/navbar_default" %>
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
      <% if devise_controller? %>
        <div class="container" style="height: calc(100vh - 75px - 50px); width: 500px; overflow: scroll;border: 3px solid #e5e5e5;margin-top: 10px;margin-bottom: 100px;" >
         <%=yield%>
        </div>
      <% else %>
        <%= yield %>
      <% end %>
      <%=render partial: "shared/footer" %>
      <%
        if(user_signed_in?)
          Analytics.page(user_id: current_user.id, name: action_name  )
        else
          Analytics.page(anonymous_id: session.id ,name: action_name )
        end
        %>

      <div id="modal-holder"></div>

    <script>
    $(document).ready(function () {
        $('.dropdown-toggle').dropdown();
    });
    </script>
  </body>
 </html>

