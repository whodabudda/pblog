<%= form_for(:comment, url: commentable_content_comments_path ) do |f| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
     <%= f.text_field :user_id, value: current_user.id, hidden: true,readonly: true%>
  </div>
  <div class="field">
    <%= f.label :commentable_type %><br>
    <%= f.text_field :commentable_type , readonly: true%>
  </div>
  <div class="field">
    <%= f.label :commentable_id %><br>
    <%= f.text_field :commentable_id, readonly: true%>
  </div>
 <div class="field">
    <%= f.label :comment_text %><br>
    <%= f.text_area :comment_text, id: :comment_text%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
